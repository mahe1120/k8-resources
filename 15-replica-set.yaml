apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: nginx
  # These labels are replica set labels.
  labels:
    app: frontend
    tier: web
spec:
  # modify replicas according to your case
  replicas: 3
  selector:
  # replica set use these labels and create the desired number of replicas
    matchLabels:
      app: frontend
      tier: web
      # Pod template
  template:
    metadata:
      labels:
        app: frontend
        tier: web
    spec:
      containers:
      - name: nginx
        image: nginx
